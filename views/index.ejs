<!doctype html>
<html>
  <head>
    <title>Auth0 SAML SP sample</title>
  </head>
  <body>
    <% if (locals.user) { %>
    <p>Logged in as <%= user.user_id %> / <%= user.email %>.</p>
    <% } %>
    <% if (locals.account) { %>
    <p>Also logged in as <%= account.user_id %> / <%= account.email %>.</p>
    <% } else { %>
      <p><a href="/login">Click here to link another account</a></p>
    <% } %>
  </body>
</html>
